package ascii_art

// var unittest = false

// artefacts. no functionality at the moment. Also the second parameter "standard" was changed in development process, so now it just checking for error, if uncommented and if package changed to main.
// func main() {
// 	if len(os.Args) < 2 {
// 		fmt.Println("Please enter some text")
// 		return
// 	}

// 	fmt.Print(AsciiToString(os.Args[1], "standard"))
// }
